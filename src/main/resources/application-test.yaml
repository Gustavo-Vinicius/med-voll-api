spring:
  application:
    name: api

  datasource:
    url: jdbc:mysql://localhost:3306/vollmed_api_test
    username: root
    password: admin
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true

server:
  port: 8082
  error:
    include-stacktrace: never
    include-message: never
    include-binding-errors: never
api:
  security:
    token:
      secret: ${JWT_SECRET:K9mX2pQrL8nVjW5tYhBcD3sEuF7aGiHz}
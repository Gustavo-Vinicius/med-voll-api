spring:
  application:
    name: api

  datasource:
    url: jdbc:mysql://localhost:3306/vollmed_api
    username: root
    password: admin
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true

server:
  port: 8082